SELECT SNUM, SNAME
FROM SALESPEOPLE
WHERE SNAME < ANY (SELECT CNAME FROM CUST);


SELECT SNUM, SNAME
FROM SALESPEOPLE S
WHERE EXISTS (
    SELECT 1 FROM CUST C
    WHERE S.SNAME < C.CNAME
);