SELECT C.CNUM, S1.SNUM AS SALESPERSON_1, S2.SNUM AS SALESPERSON_2, S3.SNUM AS SALESPERSON_3
FROM CUST C
JOIN SALESPEOPLE S1 ON C.CNUM = S1.CNUM
JOIN SALESPEOPLE S2 ON C.CNUM = S2.CNUM
JOIN SALESPEOPLE S3 ON C.CNUM = S3.CNUM
WHERE S1.SNUM != S2.SNUM AND S1.SNUM != S3.SNUM AND S2.SNUM!= S3.SNUM;