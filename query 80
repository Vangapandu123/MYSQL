SELECT SNUM, 
       (SELECT ODATE FROM ORDERS WHERE SNUM = O.SNUM ORDER BY AMT DESC LIMIT 1) AS Highest_Order_Date,
       (SELECT ODATE FROM ORDERS WHERE SNUM = O.SNUM ORDER BY AMT ASC LIMIT 1) AS Lowest_Order_Date
FROM ORDERS O
GROUP BY SNUM;