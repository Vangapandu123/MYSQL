SELECT SNUM AS ID, SNAME AS Name, CITY, 'Salesperson' AS Type 
FROM SALESPEOPLE WHERE CITY = 'London'
UNION
SELECT CNUM AS ID, CNAME AS Name, CITY, 'Customer' AS Type 
FROM CUST WHERE CITY = 'London';